// Placeholder mientras la capa de persistencia obsoleta se reactiva.
// Actualmente no se exponen repositorios en memoria porque el bridge usa factories locales.

class InMemoryDatabaseModule {
  const InMemoryDatabaseModule();
}