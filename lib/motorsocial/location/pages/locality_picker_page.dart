import 'package:flutter/material.dart';

class LocalityPickerPage extends StatelessWidget {
  const LocalityPickerPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Locality picker')));
}
