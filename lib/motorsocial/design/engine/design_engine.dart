import '../repositories/theme_repository.dart';

class DesignEngine {
  final ThemeRepository themeRepository;
  const DesignEngine(this.themeRepository);

  void initialize() {
    // domain bootstrap
  }
}
